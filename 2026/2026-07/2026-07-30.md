# 2026/07/30 Thu

## DevContainer

DevContainerが用意する環境（例：`mcr.microsoft.com/devcontainers/rust:2-1-trixie`）を使うと、デフォルトで`vscode`という権限が絞られたユーザで操作ができる

DevContainer上でLinuxのGUIアプリを起動してもWindows側ではタスクバーにアイコンはあるが画面に表示されない。⇒内部モニタと外部モニタの拡大度（150%と100%）が違っていたため。外部モニタを外したら起動した。
