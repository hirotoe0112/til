# 2026/04/21 Tue

## npm

- `npm prune`でpackage.jsonにないパッケージを削除する

## Computer Science

- パリティチェック
  - even parityは1の数が偶数なら0、奇数なら1になる
  - odd parityは1の数が奇数なら0、偶数なら1になる
  - 4×4などのテーブル形式にして、縦と横のそれぞれのパリティを計算し、合っているかどうかを確認する
- CRC
  - データ文字列を特定の多項式で割り、余りを付加して送る
  - 受信側は送られた文字列を同じ多項式で割り、余りが0ならエラーなし
