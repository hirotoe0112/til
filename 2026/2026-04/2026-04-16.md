# 2026/04/16 Thu

## Ghost (Phantom) Dependency

- `package.json`に記載していなくても、importできるライブラリのこと
  - モノレポで他のプロジェクトによってインストールされているライブラリを使う
  - あるライブラリの依存関係として入っているライブラリを推移的に使う
- pnpmやyarnはこれを防ぐ仕組みがあるが、npmにはない

## npm

- `npx depcheck`でpackage.jsonにはあるが使っていないパッケージと、使っているがpackage.jsonにないパッケージが検出できる
